extends ../layouts/main 

block content
    h2 New Author
    form(action='/authors' method='POST')
        include _forms_fields
        .form_row.form_row_end.btn_row
            a.btn.btn_danger(href="/authors") Cancel
            button.btn.btn_primary(type='submit') Create